# ── Core API ──────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9   # Para UploadFile

# ── Validación ────────────────────────────
pydantic>=2.6.0

# ── Multi-formato de archivos ─────────────
pandas>=2.2.0
openpyxl>=3.1.2           # XLSX
xlrd>=2.0.1               # XLS (legacy)
odfpy>=1.4.1              # ODS
pyarrow>=15.0.0           # Parquet

# ── HTTP cliente asíncrono (URL + API) ────
httpx>=0.27.0

# ── Tests (opcional) ──────────────────────
pytest>=8.1.0
httpx>=0.27.0             # TestClient async
